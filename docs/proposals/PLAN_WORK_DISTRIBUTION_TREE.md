# Roadmap y Backlog de Enjambre (Swarm L2 Work Distribution)

Este documento estructura el volumen de trabajo arquitectónico para el Ethos Kernel tras la adopción del modelo de trabajo "Swarm" (PnP - Plug-and-Play) en abril de 2026. 

Aquí es donde los escuadrones de ejecución **Rojo (Cursor)**, **Azul (Copilot)** y **Naranja (Claude)** reclaman sus tareas. 
Ningún agente debe saltar a tareas de la cola de otro equipo. 

> **Track Cursor (L2):** directiva operativa y cierre de ola en [`docs/collaboration/CURSOR_TEAM_CHARTER.md`](../collaboration/CURSOR_TEAM_CHARTER.md); gate de integración en [`docs/collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md`](../collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md).

> [!IMPORTANT]
> **REGLA DE TOMA DE TAREAS (SWARM):**
> 1. Al despertar, revisa tu respectiva sección.
> 2. Toma el primer bloque marcado como `[PENDING]`.
> 3. Muévelo a `[IN_PROGRESS: <Callsign>]` (ej: `[IN_PROGRESS: Rojo-1]`).
> 4. Tras la revisión y commit exitoso a tu rama, márcalo como `[DONE]`.
>
> **Nueva Directiva Estratégica (Update L1 - Abril 2026)**:
> Tras la estabilización concurrente y extracción de Lóbulos, el proyecto se encuentra en riesgo de "Mock-Hell". Las prioridades han cambiado. **Se congela el desarrollo teórico de Gobernanza/DAO**. Toda la potencia de fuego pasa a la **Inferencia Situada y Puente con Hardware Real (Nomad Bridge)**, fusionado ahora con el **Motor de Encanto Multimodal (Charm Engine)** para una interacción persuasiva y segura.
>
> **Cola sin marca `[PENDING]` (abril 2026):** toma el siguiente item sin `[DONE]` o el `[PENDING]` al final de este bloque; cierre de ejemplo en esta ola: **8.1.39** (tope ``KERNEL_PERCEPTION_PARALLEL_WORKERS``, ver listado 8.1).
>
> **Backlog §5 (fases 1–2) [DONE — Abril 2026, Swarm L2]:** (1) ``kernel_env_*``, ``perception_*``, ``perception_coercion_u_value`` en ``src/kernel_utils.py`` con reexport ``_*`` desde ``kernel``. (2) :func:`kernel_dao_as_mock` y :func:`kernel_mixture_scorer` movidos a ``kernel_utils``; ``kernel`` reexporta sin romper ``chat_server`` / ``kernel_pipeline``. *Siguiente ola (opcional):* micro-diffs desde ``_`` helpers restantes en ``kernel.py``; **§6** — orden PEP8 en módulos tocados. *Invariantes:* ``CorpusCallosum`` / ``EthicalKernel.process`` / chat async sin cambio de semántica; **CI** ``pytest tests/`` + ``ruff``.
>
> **Fase 15 (L0) — Sustracción y consolidación [DONE — Swarm L2, Abril 2026]:** retirar ruido operativo, alinear con **CI (GitHub Actions)**; entregas: `CHANGELOG.md` + `swarm_sync`. Tareas 15.0.1–15.4 (incl. doc S.2.1) entregadas; ampliaciones vía `MINOR_CONTRIBUTIONS_BACKLOG.md`. **Tarea 15.0.1 [DONE — Cursor]:** indentación / imports en `seek_internal_purpose` y `register_turn_feedback`; `py_compile src/kernel.py`; `src/MANIFEST.json` digest. **Tarea 15.0.2 [DONE — Cursor]:** sin `DEBUG: after malabs` en stream; **CI** valida. **Tarea 15.0.3 [DONE — Abril 2026, Cursor]:** unificar la línea **S.2.1** del plan (doble `**[DONE]**` en una sola entrega, sin retocar el código) — sustracción de deuda de documentación.

---

## 🔴 COLA DE EJECUCIÓN ROJO (Cursor Squad)
**Doctrina:** Alta Fricción, Integración Hardware, Refactorización Arquitectónica, Concurrencia y Streaming.

### ⚙️ Módulo 0: Estabilización Pragmática y Reducción de Deuda (P0/P1) [DONE]
*Responsabilidad: Nivel 1 (Antigravity)*
*Objetivo: Mitigar vulnerabilidades operacionales, desmonolitizar componentes críticos y lograr paridad de operaciones/tests enfocado en funcionalidad práctica.*

- **Bloque 0.1: Desmonolitización y Abstracción de `kernel.py` [DONE]**
  - Tarea 0.1.1: **Solución Práctica a E/S Sincrónica:** Migrar el pipeline de inferencia HTTP de LLMs (`httpx` sincrónico dentro del hilo worker) hacia clientes cooperativos asíncronos (`httpx.AsyncClient`). **[DONE — Abril 2026, Cursor]:** parseo JSON defensivo compartido ``_http_response_json_dict`` en ``llm_backends.py`` (Ollama + HTTP JSON sync/async); **CI** ``.github/workflows/ci.yml`` — jobs ``quality`` (``pytest tests/`` + cobertura), ``semantic-default-contract``, ``windows-smoke`` (misma suite), ``ci-all-passed``; ``workflow_dispatch`` para disparo manual.
  - Tarea 0.1.2: **Cancelación Cooperativa (Task Cancellation):** Implementar la cancelación transparente de tareas de red pendientes cuando el loop asíncrono se venza (`KERNEL_CHAT_TURN_TIMEOUT`), liberando inmediatamente memoria y slots en el Worker Pool. **[DONE — Abril 2026, Cursor]:** ``src/modules/llm_http_cancel.py`` + ``raise_if_llm_cancel_requested`` en embeddings y streaming Ollama/Anthropic; ``LLMHttpCancelledError`` propagada; tests ``tests/test_llm_backend_adapters.py``, ``tests/test_llm_http_cancel.py``; **CI** mismo gate que 0.1.1.
  - Tarea 0.1.3: Extraer la `Perception` y la lógica de ruteo ético del objeto `EthicalKernel` gigante hacia handlers aislados que aprovechen el Async I/O en lugar de abusar de `run_in_threadpool`. **[DONE — Abril 2026, Cursor]:** orquestación sync/async ``perceive``/``aperceive`` + sensores en ``kernel_lobes/text_perception_stage.py`` (``TextPerceptionStageRunner``, delegado desde ``EthicalKernel._run_perception_stage*``); perfiles límbicos ``kernel_lobes/limbic_profile_policy.py``; **vector escalar LLM** (finito/NaN-safe) vía ``kernel_lobes/perception_signals_policy.py`` + ``kernel_lobes/signal_coercion.py``; señales chat I3/I5 ``kernel_lobes/chat_turn_signal_routing.py``; **ruteo chat** (heavy/light, contexto ético, acciones) en ``kernel_lobes/chat_turn_policy.py``; tests ``tests/test_limbic_profile_policy.py``, ``tests/test_perception_signals_policy.py``, ``tests/test_chat_turn_policy.py``; CI ``.github/workflows/ci.yml`` — ``quality`` + ``windows-smoke`` = ``pytest tests/`` completo, gate ``ci-all-passed``.
  - Tarea 0.1.4: **Burst-cancel smoke (ADR 0002) — parámetros mecánicamente válidos:** Validar en ``run_burst_cancel_smoke`` que retrasos y topes sean finitos (``math.isfinite``) y con signo esperado; evitar ``NaN``/``Inf`` en ``time.sleep``. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``_validate_run_burst_params`` + tope de humo ``_MAX_BURST_SMOKE_WORKERS=4096``; ``n_workers`` estrictamente ``int``; docstring *MOCK*; negativos/tipos vía ``ValueError``/``TypeError``; pruebas ``tests/test_llm_cancel_burst_operational.py``; **CI** — ``.github/workflows/ci.yml`` (``pytest tests/``).
  - Tarea 0.1.5: **CLI `run_burst_cancel_smoke` — paridad con validación de núcleo:** Alinear ``argparse`` (``--workers`` / retrasos / ``--cancel-after``) con las mismas invariantes que ``_validate_run_burst_params``; falla antes de reservar hilos si la entrada no es finita o excede el tope. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** `scripts/eval/run_burst_cancel_smoke.py` — `type=` con ``math.isfinite`` y tope vía `MAX_BURST_SMOKE_WORKERS` (alias en ``llm_cancel_burst``); `RuntimeError`/`ValueError`/`TypeError` impresas en **stderr** y exit 1. **Validación: CI** (Bloque 0.1).
  - Tarea 0.1.6: **Sensor nudges finitud (barrera anti-NaN hacia lóbulo simpático).** **[DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/modules/sensor_contracts._clamp01`` (no-finitos → 0,5); ``merge_sensor_hints_into_signals`` (prefiltro al copiar las 5 señales canónicas) + rama ``stability_score`` con float finito y ``< 0.4`` (excl. ``bool``); ``tests/test_sensor_contracts.py::test_merge_clamps_nonfinite_incoming_signal_values``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 0.1.7: **Contrato de CLI `run_burst_cancel_smoke` (subprocess, ADR 0002).** Pruebas que ``argparse`` rechace entradas no mecánicas (exit **2**) sin arrancar el humo de hilos. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``tests/test_run_burst_cancel_smoke_script.py`` (``--delay`` no finito, ``--join-timeout`` inf, ``--workers`` fuera de rango, ``--cancel-after`` negativo). **CI** ``.github/workflows/ci.yml`` (Bloque 0.1).
  - Tarea 0.1.8: **Charm Engine (L1) — finitud hacia `gesture_plan` (anti-NaN/Inf).** Coaccionar ``caution_level``, intimidad y *streak* a valores finitos y acotados antes de nudges y de metadata enviada al puente. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/modules/charm_engine.py`` — ``_as_unit01`` / ``_as_nonneg_int_streak``; ``sculpt`` usa ``caution`` acotado para tono; pruebas ``tests/test_charm_engine.py``; traza **MOCK/EXPERIMENTAL** en :class:`StyleParametrizer`. **CI** ``.github/workflows/ci.yml`` (Bloque 0.1).
  - Tarea 0.1.9: **`kernel_utils` (MINOR backlog §5) — desmonolitización de helpers puros** (formato + entorno + coerción, sin I/O async). Misma entrega que **Fase 15.18** (tabla y cabecera §5): :func:`format_proactive_candidate_line` (ProactivePulse idle, **EXPERIMENTAL**), ``kernel_env_*``, ``perception_parallel_workers``, ``perception_coercion_u_value``; `src/kernel` reexporta vía ``_*``; pruebas ``tests/test_kernel_utils.py`` + regresión Tri-Lobe. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/MANIFEST.json``; **CI** ``.github/workflows/ci.yml`` (``pytest tests/``).
  - Tarea 0.1.10: **Regresión anti-sombra — reexport ``kernel`` ≡ ``kernel_utils`` para facades DAO/mezcla:** ``tests/test_kernel_utils.py::test_kernel_module_reexports_match_kernel_utils`` afirma identidad de objeto con :mod:`src.kernel_utils` (no otra copia de función en :mod:`src.kernel`). Complementa Plan **8.1.30** (contratos de narrowing). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** **CI** ``.github/workflows/ci.yml`` (``pytest tests/``).
- **Bloque 0.2: Escalabilidad del Chat Server [DONE]**
  - Tarea 0.2.1: Rediseñar la capa WebSocket del servidor (`chat_server.py`) para manejar concurrencia pura sin bloquear el event loop principal, permitiendo streaming asíncrono. **[DONE — Abril 2026, Cursor]:** tope UTF-8 ``KERNEL_WS_MAX_MESSAGE_BYTES``; raíz JSON obligatoriamente objeto (arrays / `true` / `null` / números / string JSON → `invalid_message_shape`); helper ``_ws_text_exceeds_utf8_byte_limit``; **paridad** ``process_chat_turn_stream`` ↔ ``process_chat_turn_async``: ``_snapshot_feedback_anchor`` + ``_release_chat_turn_id`` en caminos safety_block / kernel_block / éxito (``KERNEL_FEEDBACK_CALIBRATION`` + mensajes ``operator_feedback``); ``charm_engine.apply`` amortiguado; tests ``tests/test_chat_server.py`` (incl. ``test_websocket_non_dict_json_root_variants``); **CI** ``.github/workflows/ci.yml``: ``quality`` + ``windows-smoke`` ejecutan ``pytest tests/`` completo.
- **Bloque 0.3: Mantenimiento Histórico (Legacy Modules) [DONE]**
  - Tarea 0.3.1: Los módulos de la integración fundacional 1 al 6 (Mock DAO, Safety Interlock, UchiSoto, Swarm Logic) han sido consolidados y se consideran estables en producción local.

### 🧬 Bloque 26.0: Tri-Lobe — MemoryLobe, Bus y MotivationEngine (raíz del kernel)
*Responsabilidad: Cursor / Boy Scout — regla ``.cursor/rules/tri-lobe-implementation-standard.mdc``.*

- **Tarea 26.0.1:** Restaurar el puente **MemoryLobe ↔ ``KERNEL_EVENT_BUS``** — suscripción a ``EVENT_KERNEL_AMNESIA_FORGET_EPISODE`` → ``memory_lobe.forget_episode`` (SelectiveAmnesia + ``DAOOrchestrator`` en cascada); emisión de ``EVENT_KERNEL_EPISODE_REGISTERED`` tras ``execute_episodic_stage``. **[DONE — Abril 2026, Cursor]:** ``EthicalKernel._emit_kernel_episode_registered`` / ``_on_bus_amnesia_forget_episode``; eventos en ``src/modules/kernel_event_bus.py``; tests ``tests/test_kernel_tri_lobe_bus_memory.py``; verificación ``rg`` en regla Tri-Lobe.
- **Tarea 26.0.2:** **ProactivePulse** asíncrono sin chat externo — ``KERNEL_PROACTIVE_PULSE_IDLE_S``, ``touch_external_chat_activity`` en ``process_chat_turn_stream`` / ``process_chat_turn_async``; bucle ``asyncio`` en ``chat_server.py`` ``/ws/chat`` vía ``run_sync_in_chat_thread``; ``MotivationEngine.aupdate_drives``; ``seek_internal_purpose`` acepta ``CandidateAction``. **[DONE — Abril 2026, Cursor]:** emisión ``EVENT_KERNEL_PROACTIVE_PULSE``; **empaquetado:** ``python scripts/swarm_sync.py --msg '...' --block 26.0 --author Cursor`` tras validar tests.

### 🧠 Módulo C: Profundidad Cognitiva y Recompensas RLHF
*Responsabilidad: Nivel 2 (Team Claude)*
*Dependencias: Modelo RLHF existente y Motor Bayesiano.*

> Cola operativa y huecos de cableado (abril 2026): [`PROPOSAL_CLAUDE_TEAM_WORK_QUEUE.md`](PROPOSAL_CLAUDE_TEAM_WORK_QUEUE.md).

- **Bloque C.1: Fusión BMA (Bayesian Mixture Averaging) y Recompensas RLHF**
  - Tarea C.1.1: Conectar los *outputs* asíncronos del `rlhf_reward_model.py` directamente como *Priors* moduladores dentro de `bayesian_engine.py` en tiempo real. **[DONE — Abril 2026, Cursor]:** ``EthicalKernel.aprocess`` hace ``await apply_rlhf_modulation_to_bayesian_async(...)`` tras MalAbs y antes de ``_run_bayesian_stage``; ``process_chat_turn_stream`` / ``process_chat_turn_async`` copian ``mal.rlhf_features`` en ``signals`` tras ``merge_chat_turn_signals_for_ethical_core``; ``merge_chat_turn_signals_for_ethical_core`` preserva ``rlhf_features`` (shallow copy); ``RewardModel`` / ``maybe_modulate_from_malabs_rlhf_features`` + clip finito; modelo opcional en ``artifacts/rlhf/reward_model.json``; ver [`PROPOSAL_CLAUDE_TEAM_WORK_QUEUE.md`](PROPOSAL_CLAUDE_TEAM_WORK_QUEUE.md); **CI** ``pytest tests/`` (``.github/workflows/ci.yml``).
  - Tarea C.1.2: Validar el arrastre de métricas RLHF sobre las decisiones de los polos multipolares en el estadio 3 del Kernel. **[DONE — Abril 2026, Cursor]:** contrato de regresión — tras ``maybe_modulate_from_malabs_rlhf_features`` las ``applied_mixture_weights`` devueltas por ``BayesianInferenceEngine.evaluate`` (estadio bayesiano / mezcla tripartita que alimenta polos en lóbulo ejecutivo) difieren del baseline; sin flag no hay arrastre; tests ``tests/test_rlhf_c12_stage3_mixture_drag.py``; **CI** ``pytest tests/`` (``.github/workflows/ci.yml``).
- **Bloque C.2: Gobernanza Real en Runtime**
  - Tarea C.2.1: Implementar handlers para que cualquier voto exitoso en el `MultiRealmGovernor` altere en vivo (hot-reload) los umbrales de Absoluto Mal (`semantic_chat_gate.py`) sin necesidad de reiniciar el proceso del kernel. **[DONE — Abril 2026, Cursor]:** `MultiRealmGovernor.resolve_proposal` publica `EVENT_GOVERNANCE_THRESHOLD_UPDATED` con `realm.current_config.to_dict()` (incluye `theta_allow` / `theta_block`); `EthicalKernel._on_governance_threshold_updated` → `semantic_chat_gate.apply_hot_reloaded_thresholds`; prueba de contrato `tests/test_multi_realm_governance.py::TestGovernanceMalabsHotReloadContract::test_resolve_proposal_publishes_kernel_threshold_payload`; **CI** `.github/workflows/ci.yml` incluye este archivo vía `pytest tests/`.

### 🦾 Módulo S: Encarnación Activa y Hardware Bridge (Nomad PC/Mobile)
*Responsabilidad: Nivel 2 (Team Cursor / Team VisualStudio)*
*Dependencias: Arquitectura Somática e Inferencia de Visión estabilizada.*

- **Bloque S.1: Nomad SmartPhone LAN Bridge**
  - Tarea S.1.1: Desarrollar conectores WebSocket o WebRTC de baja latencia (`src/modules/nomad_bridge.py`) para consumir streams de video and audio desde un dispositivo móvil Android/iOS en red local, inyectando los fotogramas en el `VisionInference` de manera asíncrona. **[DONE — Abril 2026, Cursor]:** límites UTF-8 / base64 / telemetría y raíz JSON objeto + `payload` objeto en [`nomad_bridge.py`](../../src/modules/nomad_bridge.py); colas acotadas; ``KERNEL_NOMAD_WS_MAX_MESSAGE_BYTES``; contadores ``rejections`` / ``queue_evictions`` en ``nomad_bridge_queue_stats_v3``; con ``KERNEL_METRICS=1``, eventos en Prometheus (`ethos_kernel_nomad_bridge_*`). Tests: [`tests/test_nomad_bridge_stream.py`](../../tests/test_nomad_bridge_stream.py), [`test_observability_metrics.py`](../../tests/test_observability_metrics.py); **CI** ``quality`` + ``windows-smoke`` = ``pytest tests/``, gate ``ci-all-passed``.
- **Bloque S.2: Calibración Termo-Visual Continua [DONE]**
  - Tarea S.2.1: Refinar las interrupciones del `VitalityAssessment` (ej. alertas de calor del dispositivo) utilizando la telemetría real transmitida por el *Nomad Bridge*. **[DONE — Abril 2026, Cursor]:** Nomad backfill vía ``merge_nomad_telemetry_into_snapshot`` / ``KERNEL_NOMAD_TELEMETRY_VITALITY``; alias y coerción ``_coerce_nomad_telemetry_for_snapshot`` (``device_temp_c``, acelerómetro, shock); banda ``KERNEL_VITALITY_WARNING_TEMP`` / ``KERNEL_VITALITY_THERMAL_WARN_C`` + histéresis ``KERNEL_VITALITY_THERMAL_HYSTERESIS`` / ``KERNEL_VITALITY_THERMAL_HYSTERESIS_C``; campo ``thermal_elevated`` en ``VitalityAssessment``; ``KERNEL_VITALITY_IMPACT_JERK_THRESHOLD``; ``assess_vitality`` usa ``_finite_clamp01`` / ``_finite_jerk`` / ``_finite_celsius``; alias Nomad → ``core_temperature`` (``skin_temperature``, ``battery_temp_c``, …); ``apply_nomad_telemetry_if_enabled`` en ``PerceptiveLobe._chat_assess_sensor_stack`` + ``peek_latest_telemetry``; ``vitality_communication_hint`` con ``trust_level`` finito; nudges ``merge_sensor_hints_into_signals``; confianza ``build_perception_confidence_envelope``; tests [`tests/test_vitality.py`](../../tests/test_vitality.py), ``tests/test_perception_confidence.py``; **CI** ``.github/workflows/ci.yml`` (``pytest tests/`` completo).

### Fase 15: Sustracción y consolidación (Swarm)
- **Tarea 15.0.3 — Plan S.2.1 (doc, anti-duplicado):** **[DONE — Abril 2026, Cursor]:** unificar en el árbol la descripción de **S.2.1** (un solo `**[DONE]**`); sin cambio de código. **CI** inalterada; cierre: `swarm_sync` + `CHANGELOG.md`.
- **Tarea 15.1 — Higiene numérica en ruta de vitalidad (anti-orphan):** **[DONE — Abril 2026, Cursor]:** mismo núcleo que S.2.1 en ``src/modules/vitality.py``; ``assess_vitality`` y ``_chat_assess_sensor_stack`` / ``EthicalKernel`` (grep: ``assess_vitality``); cierre de bloque: ``python scripts/swarm_sync.py`` con mensaje de impacto; validación de calidad en **GitHub Actions** (``quality`` / ``windows-smoke``), no depender de colección local frágil.
- **Tarea 15.2 — Coherencia Nomad↔jerk (Fase 15):** **[DONE — Abril 2026, Cursor]:** ``_coerce_nomad_telemetry_for_snapshot`` pasa acelerómetro / shock finitos sin normalizar a ``[0,1]`` (alineado con ``_finite_jerk`` + umbral 0.8 en ``assess_vitality``); tests ``tests/test_vitality.py``; **CI** vía ``pytest tests/``; sin depender de colección local.
- **Tarea 15.3 — ProactivePulse payload (Fase 15, anti-NaN):** **[DONE — Abril 2026, Swarm L2]:** ``EthicalKernel.emit_proactive_pulse_if_idle`` serializa impacto/confianza finitos y acotados; ``idle_seconds`` finito antes de ``EVENT_KERNEL_PROACTIVE_PULSE``. Comprobación local: ``python -m py_compile src/kernel.py``; **CI** ``.github/workflows/ci.yml``; cierre: ``python scripts/swarm_sync.py --msg '...'``.
- **Tarea 15.4 — Motivation report + seek_internal (Fase 15):** **[DONE — Abril 2026, Swarm L2]:** ``_sanitize_motivation_report_for_bus`` (``motive`` en ``EVENT_KERNEL_PROACTIVE_PULSE``); ``seek_internal_purpose`` acota ``estimated_impact`` / ``confidence`` en rama ``dict`` y ``CandidateAction``. **CI** ``.github/workflows/ci.yml``; ``py_compile src/kernel.py``; ``swarm_sync``.
- **Tarea 15.5 — [DONE — Abril 2026, Boy Scout]:** ``EvidenceSafe``: clave Fernet inválida → ``except (TypeError, ValueError)`` (no ``Exception`` mudo) + ``logging.warning``; ``llm_cancel_burst._validate_run_burst_params`` (ya con ``math.isfinite``/``numbers.Real``) alineado a pruebas; ``test_evidence_safe_invalid_fernet_key_falls_back_with_warning``, ``test_run_burst_cancel_smoke_rejects_nonfinite_completion_delay``. **CI** ``.github/workflows/ci.yml``.
- **Tarea 15.6 — [DONE — Abril 2026, Boy Scout]:** ``JsonFilePersistence.load`` (``src/persistence/json_store.py``) — reemplazar ``except Exception`` por ``(InvalidToken, UnicodeDecodeError, ValueError)`` + ``logging.info`` en fallback a JSON plano (migración / clave distinta); import ``Fernet`` al módulo; prueba ``test_json_encrypted_load_fallback_plain_file_emits_info_log``. **CI** ``.github/workflows/ci.yml``.
- **Tarea 15.7 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``atomic_write_bytes`` (``src/persistence/atomic_io.py``) — manejo explícito ``(OSError, TypeError)`` (alineado a **8.1.8** / pathlib; no ``except Exception``) y re-raise; best-effort ``unlink`` de ``*.tmp.*`` vía ``_try_unlink_tmp``. **Orfan verify:** ``JsonFilePersistence.save`` y cadena de checkpoint; **CI** ``pytest tests/``; no silencio de errores.
- **Tarea 15.8 — [DONE — Abril 2026, Boy Scout / Plan 8.1.2]:** ``src/utils/terminal_colors.py`` — inicio de VT en Windows: ``except (OSError, AttributeError, TypeError, ValueError)`` + ``logging.debug`` (sustituye ``except Exception: pass`` al importar el módulo con color activo). **CI** ``tests/test_terminal_colors.py`` + gate global.
- **Tarea 15.9 — MotivationEngine: finitud + brazo INTEGRITY [DONE — Abril 2026, Swarm L2]:** ``_float_kernel_state`` y clamp en ``update_drives``; filtro ``math.isfinite`` al rankear drives y en ``get_motivation_report``; acción ``proactive_integrity_check`` para ``DriveType.INTEGRITY``; pruebas ``tests/test_motivation_engine.py``; **CI** ``pytest tests/``; cierre ``swarm_sync --block 15.9``.
- **Tarea 15.10 — [DONE — Abril 2026, Boy Scout / Bloque 0.1.1]:** ``src/modules/llm_backends.py`` — ``_http_response_json_dict`` sin ``except Exception`` (``json.JSONDecodeError`` / ``UnicodeDecodeError`` / ``ValueError``; prefijo con ``(OSError, httpx.ReadError)``); ``OllamaLLMBackend.embedding``: ``httpx.HTTPError`` + ``ValueError`` con ``logging.debug`` (sustituye retorno mudo). **CI** ``tests/test_llm_backend_adapters.py``.
- **Tarea 15.11 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``scripts/swarm_sync.py`` — ``_ollama_local_audit`` ``urlparse`` sin ``except Exception``; ``(TypeError, ValueError)`` y ``netloc`` vacío; pruebas ``test_swarm_sync_ollama_audit_uses_ollama_base_url``, ``test_swarm_sync_ollama_audit_prepends_http_for_host_only``. **CI** ``tests/test_swarm_sync_script.py``; cierre: ``swarm_sync --block 15.11``.
- **Tarea 15.12 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/modules/temporal_horizon_prior.py`` — ``_parse_ts`` sin ``except Exception``; ``(ValueError, TypeError, AttributeError)`` + guard ``isinstance(..., str)``; prueba ``test_parse_ts_invalid_and_z_suffix`` en ``tests/test_temporal_horizon_prior.py``; cierre: ``swarm_sync --block 15.12``. **CI** ``pytest tests/test_temporal_horizon_prior.py`` + suite global.
- **Tarea 15.13 — [DONE — Abril 2026, Boy Scout]:** ``_package_version`` en ``src/chat_server.py`` — ``importlib.metadata.PackageNotFoundError`` (no ``except Exception``) antes de ``"dev"``; prueba ``test_package_version_falls_back_to_dev_when_not_installed``; **CI** ``tests/test_chat_server.py``.
- **Tarea 15.14 — [DONE — Abril 2026, Boy Scout / Bloque 0.2.1]:** bucle Proactive Pulse ``/ws/chat`` — ``asyncio.CancelledError`` se re-lanza (no tragar con ``Exception``); rama ``constitution_draft`` capta solo ``(TypeError, ValueError)`` (validación de borrador, ``int(level)``). Hotfix: ``src/utils/terminal_colors.py`` — ``Term.SEP_WIDTH = 70`` (rompía import: ``_DEFAULT_BLOCK_WIDTH`` huérfano). **CI** ``.github/workflows/ci.yml``.
- **Tarea 15.15 — [DONE — Abril 2026, Boy Scout / Bloque 0.2.1]:** ``run_turn`` en ``/ws/chat`` — al notificar ``internal_error`` tras ``chat_turn_error``, el fallo de ``send_json`` se acota a ``(WebSocketDisconnect, OSError, RuntimeError, TypeError)`` (conexión cerrada / I-O / JSON no serializable), no ``except Exception``; otros errores suben. **CI** ``.github/workflows/ci.yml``.
- **Tarea 15.16 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/modules/precedent_rag.py`` — ``_save`` sin ``except Exception: pass``; ``(OSError, TypeError, ValueError)`` + ``logging.warning``; E/S UTF-8; ``dirname`` vacío → ``.``; ``_load`` ``(OSError, UnicodeDecodeError, JSONDecodeError)``; pruebas ``tests/test_precedent_rag.py``; **CI** ``pytest tests/``; cierre: ``swarm_sync --block 15.16``.
- **Tarea 15.17 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/modules/semantic_chat_gate.py`` — MalAbs ancla semántica: ``backend.embedding`` con ``except`` acotada + log **DEBUG** (antes mudo) + **HTTP/hash fallback**; ``add_semantic_anchor`` deja de tragar con ``pass`` (``WARNING`` + ``exc_info``) en almacenamiento persistente y ruta heredada; búsqueda/limpieza en tienda con **DEBUG**; arbitraje LLM: ``LLMHttpCancelledError`` re-lanzada (no fail-closed); población de anclas built-in tras definir ``_fetch_embedding_with_fallback`` (import-time seguro). Pruebas ``tests/test_semantic_chat_gate.py`` / ``tests/test_semantic_chat_gate_resilience.py``; **CI** ``pytest tests/``; cierre: ``swarm_sync --block 15.17``.
- **Tarea 15.18 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0 / MINOR_CONTRIBUTIONS_BACKLOG §5]:** ``src/kernel_utils.py`` — además de ``format_proactive_candidate_line``: ``kernel_env_truthy`` / ``kernel_env_int`` / ``kernel_env_float``, ``perception_parallel_workers``, ``perception_coercion_u_value`` (finitud vía ``math.isfinite``); ``src/kernel.py`` importa con alias ``_*`` y elimina cuerpos duplicados; corrección de imports finales duplicados (``checkpoint``/``deprecation``). Pruebas ``tests/test_kernel_utils.py``; **CI** ``pytest tests/``; cierre: ``swarm_sync --block 15.18``.
- **Tarea 15.19 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/modules/semantic_anchor_store.py`` — ruta **Chroma** (``query_neighbors``, ``delete``, ``get``, ``get_all_anchors``): deja de tragar ``Exception`` sin huella; **DEBUG** con ``exc_info``; distancia→similitud: ``float`` defensivo + ``math.isfinite`` en dist/sim (sin ``NaN``/``Inf`` en vecinos). Prueba ``test_chroma_query_neighbors_failure_emits_debug`` (requiere **chromadb** en CI, skip local si no). **CI** ``pytest tests/``; cierre: ``swarm_sync --block 15.19``.
- **Tarea 15.20 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``ChromaSemanticAnchorStore`` — ``delete`` con **DEBUG** (paridad con 15.19, cierre de ``except`` mudo); ``delete_expired`` envuelto en try + log; ``before_timestamp`` y marcas de tiempo de metadatos: ``float`` seguro + ``math.isfinite`` (evita ``NaN``/``Inf`` en comparación y ``ValueError`` silencioso). Prueba ``test_chroma_delete_failure_emits_debug``. **CI** ``pytest tests/``; cierre: ``swarm_sync --block 15.20``.
- **Tarea 15.21 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** Percepción Pydantic + biografía cerebelo: ``src/modules/perception_schema.py`` — :meth:`_LLMPerceptionPayload.model_validate` emergency fallback con ``pydantic.ValidationError`` (no ``Exception``). ``src/modules/cerebellum_biography.py`` — ``refresh_from_memory`` (``(AttributeError, TypeError, ValueError)`` + **DEBUG**); ``_persist`` (``OSError`` + **WARNING**). **CI** ``tests/test_perception_*.py``, ``tests/test_cerebellum_biography.py``; cierre: ``swarm_sync --block 15.21``.
- **Tarea 15.22 — [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/modules/semantic_anchor_store.py`` — :func:`_coerce_nonnegative_ttl_s` (finito, ``>= 0``) para ``SemanticAnchorRecord.ttl_s``, ``from_env``, ``InMemory``/``Chroma`` ``default_ttl_s``, :func:`create_anchor_store` (``NaN``/``Inf``/negativos → ``0``). Complementa Harden-In-Place con in-memory ``sim`` finito (15.x) y Chroma ``upsert`` log+re-raise. Pruebas ``tests/test_semantic_anchor_store.py``; **CI** ``pytest tests/``; cierre: ``swarm_sync --block 15.22``.

### 🧪 Bloque 16.0: Scripts de laboratorio (operador, sin contrato CI)
*Herramientas opcionales; la verdad de calidad sigue siendo* **GitHub Actions** *sobre* ``tests/``*.*

- **Tarea 16.0.1 [DONE — Abril 2026, Swarm L2]:** ``scripts/run_vision_pilot_validation.py`` — retirar stub huérfano; :class:`MockLLM` con ``acommunicate`` / ``anarrate`` **MOCK/EXPERIMENTAL** (proceso natural es async); ``math.isfinite`` en puntuación moral; **comprobación** ``python scripts/run_vision_pilot_validation.py``; cierre: ``swarm_sync``.
- **Tarea 16.0.2 [DONE — Abril 2026, Swarm L2]:** ``src/modules/variability.py`` — entradas no finitas con sustitutos explícitos; ruido NumPy no finito devuelve la base coherida; salidas post-``clip`` verificadas con ``math.isfinite``; ``__all__``; ``tests/test_variability_engine.py``; ``salience_map.py`` — ``__all__`` export; **CI** ``pytest tests/`` (``.github/workflows/ci.yml``).
- **Tarea 16.0.3 [DONE — Abril 2026, Swarm L2]:** ``somatic_markers.py`` — ``__all__``; ``_clamp01`` tipado con ``object``; ``run_cursor_integration_gate.py`` incluye ``tests/test_variability_engine.py`` (anti-orfan Bayes-ruido). **Validación: CI** (gate lista + ``pytest tests/`` completo).
- **Tarea 16.0.4 [DONE — Abril 2026, Swarm L2]:** ``src/utils/terminal_colors.py`` — ``__all__``; ``_clamped_header_bar_width`` (tope :data:`_MAX_HEADER_BAR` = 512, alineado a **8.1.13**; negativos/bool/overflow / no finito → ancho seguro); prueba ``tests/test_terminal_colors.py::test_header_bar_width_clamped_and_safe``; gate: ``run_cursor_integration_gate.py`` + ``tests/test_terminal_colors.py``. **CI** ``pytest tests/``.
- **Tarea 16.0.5 — PerceptiveLobe MOCK + ``aclose`` [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``observe_async`` documentada como **MOCK/EXPERIMENTAL**; sin TODO engañoso; ``sensory_latency_lag`` con ``math.isfinite`` + tope; ``perception_confidence=1.0`` alineado a ``tests/test_perceptive_lobe.py``; :meth:`aclose` para ``CorpusCallosumOrchestrator`` / cierre; ``dict[str, Any]`` en ``execute_stage``; dedupe imports en ``kernel_lobes/__init__.py``. **CI** ``tests/test_perceptive_lobe.py``; cierre: ``swarm_sync``.
- **Tarea 16.0.6 [DONE — Abril 2026, Swarm L2]:** ``run_cursor_integration_gate.py`` incluye ``tests/test_perceptive_lobe.py`` (anti-orfan con PLAN 16.0.5 / Tri-Lobe); ``docs/collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md`` alineada. **CI** ``pytest tests/`` + gate script.
- **Tarea 16.0.7 [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** Coherencia documental — tarea **16.0.4** en el árbol (tope 200 desactualizado) alineada a :data:`_MAX_HEADER_BAR` = 512 y **8.1.13**; segunda tarea **8.1.16** duplicada renumerada a **8.1.17** (``Term.highlight_impact`` / ``OverflowError``). Sin cambio de runtime; **CI** inalterada. Cierre: ``swarm_sync``.
- **Tarea 16.0.8 [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``run_cursor_integration_gate.py`` incluye ``tests/test_salience_map.py`` y ``tests/test_somatic_markers.py`` (paridad con **8.1.4** / **8.1.6**; anti-orfan del gate). ``docs/collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md`` alineado. **CI** global inaltera; el script de gate operador cubre ahora GWT-lite + somatic finitos. Cierre: ``swarm_sync --block 16.0.8``.
- **Tarea 16.0.9 [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** Anti-orfan **26.0.1** — el gate operador añade ``tests/test_kernel_tri_lobe_bus_memory.py`` (MemoryLobe ↔ ``EVENT_KERNEL_AMNESIA_FORGET_EPISODE`` / ``EVENT_KERNEL_EPISODE_REGISTERED``) junto a 16.0.8. ``docs/collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md`` alineada. Cierre: ``swarm_sync --block 16.0.9``.
- **Tarea 16.0.10 [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** El gate añade ``tests/test_kernel_lobes_stack.py`` (cubre **8.1.5** nudge finito en lóbulo límbico, **8.1.24** ProactivePulse / ``_format_proactive_candidate_line`` y ruta ``CorpusCallosum`` idle; anti-orfan respecto a Tri-Lobe ya listado en 16.0.5/16.0.9). **CI** ``pytest tests/`` + script de gate. Cierre: ``swarm_sync --block 16.0.10``.
- **Tarea 16.0.11 [DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** El gate añade ``tests/test_kernel_utils.py`` (**15.18** / **0.1.9-0.1.10** / **8.1.30-8.1.31** — facades puros, reexport ``kernel``≡``kernel_utils``, ``kernel_env_float`` no finito → default). Alinea GWT del operador con desmonolitización de ``kernel.py``. Cierre: ``swarm_sync --block 16.0.11``.

### 🧹 Módulo 8: Higiene, Mantenimiento y Deuda Menor
*Responsabilidad: Nivel 2 (Team Copilot / Contribuidores Libres)*
*Nota:* El registro exhaustivo de estas tareas debe consolidarse permanentemente en el `MINOR_CONTRIBUTIONS_BACKLOG.md`.

- **Bloque 8.1: Calidad y DX (Developer Experience)**
  - Tarea 8.1.1: Linter continuo y auditoría de `docstrings` / `type hints` a lo largo de las divisiones de `kernel.py`. **[DONE — Abril 2026, Cursor]:** ``LimbicPerceptionProfile`` (``TypedDict``) + ``kernel_lobes/limbic_profile_policy.py``; ``TextPerceptionStageRunner`` / ``PerceptionStageResult``; ``kernel_lobes/chat_turn_policy.py`` (``prioritized_principles_for_context``); tests ``tests/test_limbic_profile_policy.py``, ``tests/test_chat_turn_policy.py``; **CI** ``quality`` + ``windows-smoke`` = ``pytest tests/`` en ``.github/workflows/ci.yml``.
  - Tarea 8.1.2: Refactorización y embellecimiento de las salidas ANSI de terminal para facilitar el modo de depuración de operadores locales. **[DONE — Abril 2026]:** ``Term.SEP_WIDTH``, ``rule_heavy`` / ``rule_light``, ``Term.header`` / ``Term.subheader``; ``NO_COLOR`` + ``KERNEL_TERM_COLOR`` vía ``_colors_enabled()``; VT Windows; tests ``tests/test_terminal_colors.py``; **CI** push/PR o ``workflow_dispatch`` — ``quality`` + ``windows-smoke`` + ``semantic-default-contract`` + ``compose-validate`` + ``ci-all-passed``.
  - Tarea 8.1.3: Extender mocks para *input_trust* (ej. Caracteres homoglyphs cirílicos para evadir la puerta de Absoluto Mal) **[DONE — Abril 2026]:** matriz homoglífica en ``tests/fixtures/input_trust_homoglyphs.py`` (incl. IE/о en *make/bomb*, omicron griego en *how*); ``normalize_text_for_malabs`` + controles C0; **CI** ``quality`` (``pytest tests/`` Ubuntu matrix) + **Windows** misma suite + ``ruff format``; push/PR a ``main`` y ``master-antigravity`` / ``master-Cursor`` / ``master-claude``.
  - Tarea 8.1.4: **SalienceMap finitud** — entradas no finitas o fuera de ``[0,1]`` en señales / ``InternalState`` / reflejo no rompen la normalización. **[DONE — Swarm L2, Abril 2026]:** ``_fin_unit_interval`` + fallback uniforme ``1/|axes|`` (corrige suma 1.25 con 5 ejes); regresión ``tests/test_salience_map.py``; **hotfix** ``EthicalKernel.aprocess``: ``KernelDecision`` fuera del ``else`` de ``register_episode`` (``UnboundLocalError`` con ``register_episode=True``). **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.5: **Lóbulo límbico / swarm trust nudge** — ``get_swarm_trust_nudge()`` pasa por ``safe_signal_scalar``; errores de llamada restringidos a ``TypeError`` / ``ValueError`` / ``AttributeError`` (no bare ``Exception``). **[DONE — Abril 2026]:** evita que ``+inf``/``NaN`` empuje ``trust`` a ``1.0`` vía suma; ``tests/test_kernel_lobes_stack.py``. **Validación: GitHub Actions** (``pytest tests/``).
  - Tarea 8.1.6: **Somatic markers (v10) — señales finitas en nudges** — ``_finite_signal01`` + ``_clamp01``; ``learn_negative_pattern`` ignora pesos no finitos; ``apply_somatic_nudges`` toma bases ``risk``/``urgency``/``calm`` finitas antes de sumar con el peso almacenado (alinea con 8.1.4 / ``SalienceMap``). **[DONE — Abril 2026, Pragmatismo V4.0]:** ``src/modules/somatic_markers.py``; pruebas ``tests/test_v10_operational.py``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.7: **Operador — inventario ``KERNEL_RLHF_*`` (puente C.1):** Alinear ``.env.example`` y ``docs/ENV_VAR_CATALOG.md`` con las variables realmente leídas en ``src/`` (entrenamiento, modulación bayesiana, escalas). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** catálogo §3.1; comentarios en ``.env.example``; **CI** inalterada.
  - Tarea 8.1.8: **Checkpoint atómico (`atomic_write_bytes`) — excepciones y cleanup de `*.tmp.*`:** No engullir ``Exception`` genérico; ``_try_unlink_tmp`` para el sibling temporal. **[DONE — Abril 2026, Pragmatismo V4.0]:** `src/persistence/atomic_io.py` — ``(OSError, TypeError)``; pruebas ``test_atomic_write_bytes_persists_content`` / ``test_atomic_write_bytes_cleans_tmp_on_replace_failure``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.9: **Entrada de operador — ``AGENTS.md`` enlaza el catálogo de env (anti-huérfano 8.1.7):** Una línea en *Read first* a ``docs/ENV_VAR_CATALOG.md`` (incl. §3.1 ``KERNEL_RLHF_*`` / C.1) y puntero a ``.env.example``. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** sin cambio de runtime; **CI** inalterada.
  - Tarea 8.1.10: **Charm Engine — contrato de ``frustration_streak`` (racha no negativa, finita):** pruebas para ``_as_nonneg_int_streak`` (``NaN``/``Inf``/negativo/overflow a ``int`` → 0; strings numéricos válidos); docstring de módulo en inglés + explícito **EXPERIMENTAL/MOCK** en pipeline de estilo. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``tests/test_charm_engine.py``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.11: **``Term.highlight_impact`` — no finitos / coerción:** evitar que ``NaN``/``Inf`` hereden verde o rojo por comparaciones; ``object()`` / no numérico → ``?`` atenuado. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py`` (``math.isfinite`` + ``try``/``float``); ``tests/test_terminal_colors.py``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.12: **``Term.highlight_decision`` — modos canónicos:** ``str(...).strip()``; cadena vacía o solo blancos → ``?`` atenuado; coherente con 8.1.11. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; pruebas incl. ``test_highlight_decision_whitespace_trim_and_empty``; ajuste de regresión a ``_MAX_HEADER_BAR`` (512) en ``test_header_bar_width_clamped_and_safe``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.13: **``Term.header`` — unificación ``_clamped_header_bar_width`` (V4.0):** un solo camino; tope ``_MAX_HEADER_BAR = 512``; ``default=Term.SEP_WIDTH``; ``bool``/no finito inválido → ancho seguro; eliminada duplicación con segundo helper. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``__all__`` incluye ``_MAX_HEADER_BAR``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.14: **``_clamped_header_bar_width`` — cadenas numéricas (operador / env):** Aceptar ``str`` (p. ej. ``"  42  "``) vía ``float``→``int``; cadenas no numéricas → ``default``; mantiene tope y finitud. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_header_bar_width_accepts_numeric_str`` / ``test_header_bar_width_bad_str_falls_back_to_default``; **CI** ``.github/workflows/ci.yml**.
  - Tarea 8.1.15: **``_clamped_header_bar_width`` — ancho ``int`` nativo (sin ``float(n)`` post-``int``):** el camino unificado 8.1.13 hacía ``float(w)`` al final, de modo que ``float(10**400) == inf`` vaciaba el separador ``═``. Rama ``int`` explícita; ``str``/``float`` con finitud antes de fijar ``w``; ``None`` al ``default``; ``highlight_impact`` acepta ``OverflowError`` en coerción. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_header_bar_width_huge_int_does_not_use_float_of_int``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.16: **``operator.index`` + tipos públicos (Boy Scout / DX):** rama final de ``_clamped_header_bar_width`` sin ``int(raw)  # type: ignore``; ``Term.highlight_impact(val: object)`` alineado a coerción real; tests sin ``# type: ignore`` en anchos/``object()``. **[DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``tests/test_terminal_colors.py``; **CI** ``.github/workflows/ci.yml``; cierre: ``swarm_sync``.
  - Tarea 8.1.17: **``Term.highlight_impact`` — contrato ``OverflowError`` explícito:** unión de excepciones con ``float()`` incluye ``OverflowError``; prueba con ``__float__`` patológico. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_highlight_impact_overflow_in_float_protocol_falls_back``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.18: **``_clamped_header_bar_width`` en ``__all__`` (API operador / demos):** Exponer el mismo criterio de ancho acotado que :meth:`Term.header` para scripts y gates sin duplicar lógica. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_clamped_header_bar_width_is_public_and_matches_header_logic``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.19: **``rule_heavy`` / ``rule_light`` — mismo tope que ``header``:** :meth:`Term._rule_width` aplica :func:`_clamped_header_bar_width` a ``SEP_WIDTH``; argumento opcional ``width=`` (por llamada) con el mismo clamp; subclase con ``SEP_WIDTH`` enorme → como máximo :data:`_MAX_HEADER_BAR`. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_rule_heavy_and_light_respect_clamped_width``, ``test_rule_bars_clamp_when_subclass_sep_width_huge``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.20: **``Term.highlight_decision(mode: object)`` — contrato real del kernel:** la firma refleja ``None`` y etiquetas no-``str``; prueba ``None`` → ``?`` en ``test_highlight_decision_whitespace_trim_and_empty``. **[DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; **CI** ``.github/workflows/ci.yml``; cierre: ``swarm_sync``.
  - Tarea 8.1.21: **``atomic_io`` — API explícita y regresión ``TypeError`` (pathlib):** ``__all__``; prueba de que ``data`` no-``bytes`` provoca el ``TypeError`` de ``write_bytes`` sin dejar `*.tmp.*` huérfano (alinea **15.7** con implementación). **[DONE — Abril 2026, Pragmatismo V4.0]:** ``src/persistence/atomic_io.py``; ``test_atomic_write_bytes_typeerror_for_non_bytes_no_orphan_tmp``; plan **15.7** corregido (``ValueError`` → ``TypeError``). **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.22: **8.1.19 cierre de implementación — ``rule_heavy`` / ``rule_light`` aceptan ``width=`` + ``_DEFAULT_SEP_WIDTH``:** Tarea 8.1.19 (plan) requería ``width`` opcional; la firma aún no lo aceptaba (**TypeError** en ``test_rule_heavy_and_light_respect_clamped_width``). Añadido; constante módulo ``_DEFAULT_SEP_WIDTH`` sustituye literales ``70`` en ``Term.SEP_WIDTH``, :meth:`Term._rule_width`, y default de :meth:`Term.header` (coherente con Pragmatismo / anti-drift). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.23: **Regresión ``rule_*``/``width`` — nombre alineado al plan:** La prueba de ancho opcional se llama ``test_rule_heavy_and_light_respect_clamped_width`` (referencia cruzada 8.1.19/22). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``tests/test_terminal_colors.py``; **CI** inalterada.
  - Tarea 8.1.24: **Tri-Lobe / ``CorpusCallosumOrchestrator`` — ProactivePulse (idle) operador finito + import ``semantic_chat_gate``:** ``_format_proactive_candidate_line`` en ``src/kernel.py``; ``estimated_impact`` / ``confidence`` con ``math.isfinite``, exclusión de ``bool``, acotación a impacto ``[-1,1]`` y confianza ``[0,1]``; descripción en una sola línea (tope 200). ``_async_proactive_pulse`` formatea la cabecera ``ProactivePulse:`` vía el helper. **Boy Scout (import):** ``_populate_builtin_anchors_to_store`` y el bloque ``try``/``except`` al importar solo **después** de ``_fetch_embedding_with_fallback`` (evita ``NameError`` y doble definición). Pruebas ``tests/test_kernel_lobes_stack.py`` (``test_format_proactive_candidate_line_clamps_nonfinite_impact_conf`` / ``test_corpus_callosum_proactive_idle_uses_finite_operator_line``). Cadenas operador **EXPERIMENTAL** en ruta idle. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** **CI** ``.github/workflows/ci.yml`` (``pytest tests/``).
  - Tarea 8.1.25: **``Term.header`` — ancho por omisión = :meth:`_rule_width` (paridad con reglas y subclases):** Sustituye el default fijo (``_DEFAULT_SEP_WIDTH`` en la firma) por ``width=None`` y rama ``_rule_width()``; ``Term`` / subclase con ``SEP_WIDTH`` distinto obtiene la misma anchura en cabecera que en ``rule_heavy``/``rule_light`` sin argumentos. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_header_default_width_follows_subclass_sep_width``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.26: **``Term.header`` — *width* explícito: ``default=`` = :meth:`_rule_width` (no ``SEP_WIDTH`` bruto):** :func:`_clamped_header_bar_width` devuelve ``default`` sin re-trocear en rutas ``bool``/error; con ``default=cls.SEP_WIDTH`` (p. ej. 9999) un *width* inválido reventaba el tope. **Harden (8.1.25 follow-up):** ``default=cls._rule_width()``. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_header_invalid_width_fallback_uses_clamped_default``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.27: **``_clamped_header_bar_width`` — *default* siempre saturado:** Añadido :func:`_saturate_bar_width`; toda rama (incl. fallback a *default* por ``bool``/``None``/error) pasa por ``[1, _MAX_HEADER_BAR]``; doble cinturón con 8.1.26. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_clamped_header_bar_width_saturates_default_on_invalid_raw``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.28: **``Term.subheader(title: object)`` — paridad con :meth:`highlight_decision`:** coerción ``str``/``strip``; ``None``/solo blancos → ``?`` atenuado; títulos no-``str`` etiquetables (p. ej. enteros). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_subheader_coercion_matches_decision_contract``; **CI** ``.github/workflows/ci.yml**.
  - Tarea 8.1.29: **Gate de integración Cursor — V14 ``swarm_sync`` en ``TEST_TARGETS``:** ``tests/test_swarm_sync_script.py`` añadido a ``scripts/eval/run_cursor_integration_gate.py``; doc en ``docs/collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md``; asegura que el hand-off JSON (``--dry-run``, `local_llm_audit`, ``OLLAMA_BASE_URL``) no se desincronice del script operador. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** **CI** global inaltera; cierre: ``swarm_sync``.
  - Tarea 8.1.30: **``kernel_utils`` (MINOR_CONTRIBUTIONS_BACKLOG §5) — contrato anti-drift en narrowing helpers:** pruebas ``kernel_dao_as_mock`` (``MockDAO`` plano vs ``DAOOrchestrator.local_dao``), ``kernel_mixture_scorer`` (``BayesianEngine.scorer`` vs fachada ``WeightedEthicsScorer``), y ``format_proactive_candidate_line`` con ``estimated_impact`` ``bool`` (no tratado como ``1.0``; alinea con 8.1.24 / Tri-Lobe). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``tests/test_kernel_utils.py``; **CI** ``.github/workflows/ci.yml`` (``pytest tests/``).
  - Tarea 8.1.31: **``kernel_env_float`` — Harden-In-Place (``math.isfinite`` en env):** :func:`kernel_env_float` en ``src/kernel_utils.py`` devuelve el *default* del llamador si el parseo a ``float`` no es finito (``NaN``/``±Inf``), alineado a 8.1.24/ProactivePulse (``KERNEL_PROACTIVE_PULSE_IDLE_S`` y futuros flotantes de entorno). Prueba ``test_kernel_env_float_nonfinite_falls_back``; **CI** ``.github/workflows/ci.yml`` (``pytest tests/``). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0].** Cierre: ``swarm_sync --block 8.1.31``.
  - Tarea 8.1.32: **``_DEFAULT_SEP_WIDTH`` en :data:`__all__` (operador / gates):** Constante de ancho base del módulo exportada junto a :data:`_MAX_HEADER_BAR` y :func:`_clamped_header_bar_width`; prueba ``test_default_sep_width_export_matches_base_class`` (``_DEFAULT_SEP_WIDTH == Term.SEP_WIDTH`` en la clase stock). **[DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``tests/test_terminal_colors.py``; **CI** ``.github/workflows/ci.yml``; cierre: ``swarm_sync``.
  - Tarea 8.1.33: **Prior temporal (ADR 0005) — señales finitas hacia ``BayesianEngine.hypothesis_weights``:** :func:`compute_horizon_signals` asegura ``np.std`` y estabilidad a largo plazo con ``math.isfinite``; retorno atómico ``(0.0, 0.5, 0.0)`` si **cualquiera** de ``weeks_trend`` / ``long_term_stability`` / ``combined`` no es finito (p. ej. racha reciente con ``ethical_score`` solo ``NaN``). :func:`apply_horizon_prior_to_engine` pasa un ``combined`` finito a :func:`_nudge_delta` (defensa en profundidad). Pruebas ``tests/test_temporal_horizon_prior.py`` (``test_compute_horizon_signals_all_nan_scores_falls_back`` con ≥3 episodios recientes; ``test_apply_prior_nonfinite_combined_skips_nudge``). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/modules/temporal_horizon_prior.py``; **CI** ``.github/workflows/ci.yml``; cierre: ``swarm_sync --block 8.1.33``.
  - Tarea 8.1.34: **``Term.header(title: object)`` — paridad con 8.1.28 / sin ``None.strip``:** coerción ``str``/``strip``; ``None``/solo blancos → ``?`` en la línea de título. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``test_header_title_coercion_object_like_subheader``; **CI** ``.github/workflows/ci.yml``.
  - Tarea 8.1.35: **``Term.color(text: object)`` — contrato documentado + regresión:** docstring (coerción vía :func:`str`); prueba ``test_color_coerces_non_str_plain_and_ansi`` (``42``/``0.5`` en modo plano; ANSI con entero). **[DONE — Abril 2026, Swarm L2, Pragmatismo V4.0]:** ``src/utils/terminal_colors.py``; ``tests/test_terminal_colors.py``; **CI** ``.github/workflows/ci.yml``; cierre: ``swarm_sync``.
  - Tarea 8.1.36: **Gate de integración Cursor — ``test_input_trust`` en ``TEST_TARGETS`` (anti-orfan 8.1.3):** Añadido ``tests/test_input_trust.py`` a ``scripts/eval/run_cursor_integration_gate.py``; documento ``docs/collaboration/CURSOR_CROSS_TEAM_INTEGRATION_GATE.md`` alineado. El operador pre-merge ahora valida el borde homoglifo / normalización hacia MalAbs. **CI** global inaltera (``pytest tests/``). **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** cierre: ``swarm_sync --block 8.1.36``.
  - Tarea 8.1.37: **``perception_coercion_u_value`` — exclusión de ``bool`` (§5 / 8.1.30 coherencia):** en ``src/kernel_utils.py`` devuelve ``None`` si *raw* es ``bool`` (``float(True)==1.0`` no debe mapear a incertidumbre = 1,0). Prueba ``test_perception_coercion_u_value_rejects_bool``; alinea a ``format_proactive`` / 8.1.24. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** **CI** ``.github/workflows/ci.yml`` (``pytest tests/``).
  - Tarea 8.1.38: **Tests prior temporal (ADR 0005) — sin I/O lenta vía ``register``:** ``test_registered_episodes_produce_nonzero_signal`` evita seis :meth:`NarrativeMemory.register` (HTTP embeddings + ``save_episode``) con ``NarrativeEpisode`` sintéticos; helper :func:`_declining_episodes_synthetic` documenta el riesgo de Ollama timeout. Aserción conservada. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** ``tests/test_temporal_horizon_prior.py``; cierre: ``swarm_sync --block 8.1.38``.
  - Tarea 8.1.39: **``Term.color`` — ``str(text)`` defensivo (8.1.35 follow-up, paridad :meth:`header`):** :meth:`Term.color` aplica ``try``/``(TypeError, ValueError)`` a la coerción; ``"?"`` si ``__str__`` patológico; un solo camino plano/ANSI. Prueba ``test_color_str_coercion_failure_falls_back_to_question``; **CI** ``.github/workflows/ci.yml``. **[DONE — Abril 2026, Cursor, Pragmatismo V4.0]:** cierre: ``swarm_sync --block 8.1.39``.

### ⚪ Módulo 9: Nomadismo Perceptivo (streaming) — visión continua [DONE]
*Responsabilidad prioritaria Team Cursor en el árbol histórico; coordinar con L1 antes de duplicar pipelines.*

- **Bloque 9.1: Daemon de Visión Continua (CNN/Webcam) [DONE]**
  - Tarea 9.1.1: `VisionInferenceEngine.analyze_jpeg_bytes` + ``VisionContinuousDaemon`` (~5 Hz, ``KERNEL_VISION_DAEMON_HZ``) con inferencia en ``ThreadPoolExecutor``; cola espejo thread-safe ``NomadBridge.vision_queue_threadsafe`` (``nomad_bridge_queue_stats_v4`` / ``vision_sync_queued``); CNN opcional ``KERNEL_VISION_DAEMON_CNN``; gate de arranque ``KERNEL_VISION_CONTINUOUS_DAEMON``; ``NomadVisionConsumer`` usa ``queue.Queue`` vía ``asyncio.to_thread``. Tests: [`tests/test_vision_continuous_daemon.py`](../../tests/test_vision_continuous_daemon.py).

---

## 🔵 COLA DE EJECUCIÓN AZUL (Team Copilot)
**Doctrina:** CI/CD Sentinel, Repo Higiene, Pruebas y Deuda Técnica Menor, Boy Scout Paranoico.

- **Bloque 8.1: Linter Continuo y Hardening Vertical [PENDING]**
  - Tarea 8.1.1: Auditar `docstrings` y `type hints` en las divisiones de `src/kernel.py` y `src/kernel_lobes/`. Introducir `try/except` donde fallen por variables nulas. 
- **Bloque 9.3: Refactorización Asíncrona Total de Eferencia [PENDING]**
  - Tarea 9.3.1: Eliminar cuellos de botella síncronos en las llamadas a utilidades de API. Migrar `http_fetch_ollama_embedding` a dependencias puramente `async` con `httpx.AsyncClient`.
- **Bloque 8.2: Hardening de Tests Mocks (Input Trust) [PENDING]**
  - Tarea 8.2.1: Extender mocks para *input_trust* (ej. inyectar simulaciones de ataques homoglyphs cirílicos para evadir la puerta de Absoluto Mal y validar la defensa).

---

## 🟠 COLA DE EJECUCIÓN NARANJA (Team Claude)
**Doctrina:** Matemática Bayesiana Avanzada, Modelado Cognitivo, Identidad Persistente y RLHF.

- **Bloque C.1: Fusión BMA y Recompensas RLHF [PENDING]**
  - Tarea C.1.1: Conectar matemáticamente los *outputs* asíncronos del `rlhf_reward_model.py` como *Priors* moduladores fuertes dentro de `src/modules/bayesian_engine.py`.
- **Bloque 9.2: Acumulación de Tensión Límbica Estática [PENDING]**
  - Tarea 9.2.1: Evolucionar el `BayesianEngine` para que pueda integrar un peso negativo de tiempo (decay) si el Lóbulo Perceptivo dicta que un estímulo peligroso (ej. arma) se mantiene visible en el stream visual +5 segundos sin voz humana.
- **Bloque 11.1: Tránsito Subjetivo del Afecto [PENDING]**
  - Tarea 11.1.1: Consolidar las fórmulas del "Espejo Roto" (Trauma) en la identidad central (`identity_reflection.py`), para asegurar que cambie los *multipliers* de utilitarismo en etapas posteriores del modelo moral.

## 🚀 Flujo de Sincronización Recomendado

1. **Semana Actual:** Antigravity (N1) se encarga transversalmente de desmonolitizar `kernel.py` y actualizar las dependencias de concurrencia. Claude y Cursor toman sus ramas (`master-claude`, `master-Cursor`) and abordan C.1 y S.1 respectivamente.
2. **Semana Siguiente:** Sincronización de progresos a `master-antigravity`. Antigravity evalúa el impacto del async I/O en la latencia global del kernel.
3. **Validación N0:** Integración unificada a `main` tras validaciones somáticas.

---

## 🟢 CERRADOS (Histórico de Producción)
*Misiones completadas por el Swarm L2 bajo supervisión L1 (referencia rápida; el detalle sigue en secciones `[DONE]` arriba).*

- Bloque 0.1: Desmonolitización y Abstracción de `kernel.py` (Antigravity/Swarm) `[DONE]`
- Bloque 0.2: Escalabilidad del Chat Server HTTP/ASGI `[DONE]`
- Bloque 10.2: Tribunal Ético Edge & MalAbs local `[DONE]`
- Bloque 10.3: Amortiguación Afectiva (Ganglios Basales EMA) `[DONE]`
- Bloque 12.1: Recursive Narrative Memory (Consolidación) `[DONE]`
