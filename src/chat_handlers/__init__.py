"""
Chat server handler modules — extracted from :mod:`src.chat_server`.

Splits the monolithic chat_server.py into cohesive handler groups:

* :mod:`.lan_governance` — LAN governance batch collectors
* :mod:`.field_control` — Phone relay / field control helpers (future)
* :mod:`.http_routes` — GET / health / DAO meta routes (future)
"""
